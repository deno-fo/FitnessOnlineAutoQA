# Запуск Android-автотестов на реальных устройствах
Инструкция предназначена для тестировщиков, которые раньше не работали с Java, Maven и Appium.

## 1. Что нужно установить
На компьютере должны быть установлены:
1. Git: https://git-scm.com/downloads
2. IntelliJ IDEA: https://www.jetbrains.com/idea/download/
3. Java JDK 17: https://adoptium.net/
4. Node.js 24: https://nodejs.org/
5. Android Studio: https://developer.android.com/studio
6. Appium 3.5.2
7. Android-драйвер Appium UiAutomator2 8.1.0
Appium и драйвер Appium UiAutomator2 будут установлены позже по этой инструкции.


Также понадобятся:
- один или несколько реальных Android-телефонов;
- USB-кабель для каждого телефона;
- установленное приложение FitnessOnline;
- доступ к приватному репозиторию GitHub;
- интернет при первой загрузке проекта.

Отдельно устанавливать Maven, npm и ADB не нужно:
- Maven проект скачает автоматически;
- npm устанавливается вместе с Node.js;
- ADB устанавливается вместе с Android SDK Platform-Tools.

## 2. Получение доступа к проекту
Репозиторий приватный. Без приглашения открыть или скачать его нельзя.

### Что должен сделать владелец проекта
1. Узнать имя пользователя тестировщика на GitHub.
2. Открыть репозиторий.
3. Перейти в `Settings` → `Collaborators`.
4. Нажать `Add people`.
5. Найти тестировщика по имени пользователя.
6. Отправить приглашение.

### Что должен сделать тестировщик
1. Открыть письмо или уведомление от GitHub.
2. Принять приглашение.
3. После этого репозиторий станет доступен его аккаунту.

## 3. Установка и настройка Android SDK

### Установка компонентов Android SDK
1. Откройте Android Studio.
2. Откройте `Settings`.
3. Перейдите в `Languages & Frameworks` → `Android SDK`.
4. Установите любую актуальную версию `Android SDK Platform`.
5. Откройте вкладку `SDK Tools`.
6. Установите `Android SDK Platform-Tools`.

В верхней части окна будет указан путь к Android SDK.

Стандартный путь на Mac:
```text
/Users/имя-пользователя/Library/Android/sdk
```

Стандартный путь на Windows:
```text
C:\Users\имя-пользователя\AppData\Local\Android\Sdk
```

### Настройка на Mac
Откройте системное приложение `Терминал`:
1. Нажмите `Command + Space`.
2. Введите `Terminal`.
3. Откройте найденное приложение.

Выполните команды по очереди:
```bash
echo 'export ANDROID_HOME="$HOME/Library/Android/sdk"' >> ~/.zshrc
echo 'export PATH="$ANDROID_HOME/platform-tools:$PATH"' >> ~/.zshrc
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
source ~/.zshrc
```

Проверьте ADB:
```bash
adb version
```

Если появилась информация о версии Android Debug Bridge, настройка выполнена.
Если команда не найдена, полностью закройте и заново откройте системный Терминал.

### Настройка на Windows
1. Откройте поиск Windows.
2. Найдите `Изменение системных переменных среды`.
3. Нажмите `Переменные среды`.
4. Создайте переменную `ANDROID_HOME`.
5. Укажите путь к Android SDK, например:
```text
C:\Users\имя-пользователя\AppData\Local\Android\Sdk
```
6. Найдите переменную `Path`.
7. Добавьте:
```text
%ANDROID_HOME%\platform-tools
```
8. Создайте переменную `JAVA_HOME`.
9. Укажите путь к установленному JDK 17.
10. Добавьте в `Path`:
```text
%JAVA_HOME%\bin
```
11. Сохраните изменения.
12. Закройте и заново откройте Командную строку.

Проверьте ADB:
```text
adb version
```

Если команда всё ещё не найдена, перезапустите компьютер.
Для некоторых моделей телефонов на Windows может дополнительно потребоваться USB-драйвер производителя. На Mac отдельный USB-драйвер обычно не требуется.

## 4. Установка Appium
1. На Mac откройте системный Терминал, на Windows откройте Командную строку. 
2. Установите Appium:
```bash
npm install --global appium@3.5.2
```
3. Установите Android-драйвер Appium:
```bash
appium driver install uiautomator2@8.1.0
```
4. Проверьте установку:
```bash
appium driver doctor uiautomator2
```
5. Если в результате указано:
```text
0 required fixes needed
```
обязательные компоненты установлены.

Эта настройка выполняется только один раз.
После установки Appium полностью перезапустите IntelliJ IDEA.

## 5. Загрузка проекта
1. Откройте IntelliJ IDEA.
2. На стартовом экране нажмите `Get from VCS` или `Clone Repository`.
3. Вставьте адрес:
```text
https://github.com/deno-fo/FitnessOnlineAutoQA.git
```
4. Нажмите `Clone`.
5. Если IDEA попросит войти в GitHub, войдите под аккаунтом, которому выдали доступ.
6. Разрешите IntelliJ IDEA доступ к GitHub.
7. Откройте загруженный проект.
8. Дождитесь завершения загрузки Maven-зависимостей.

При первой загрузке это может занять несколько минут.

## 6. Где вводить команды проекта
Команды запуска тестов нужно вводить в терминале внутри IntelliJ IDEA.

Чтобы открыть его:
1. В верхнем меню IDEA нажмите `View`.
2. Выберите `Tool Windows`.
3. Нажмите `Terminal`.

Вкладка `Terminal` также может находиться в нижней части окна IDEA.
Перед вводом команды убедитесь, что слева от курсора присутствует название проекта:
```text
FitnessOnlineAutoQA
```

Пример:
```text
admin@MacBook FitnessOnlineAutoQA %
```
Если названия проекта нет, терминал открыт не в папке проекта. Закройте его и откройте заново через IntelliJ IDEA.

## 7. Обновление проекта
Перед запуском тестов получите последнюю версию проекта:
1. В верхнем меню IDEA откройте `Git`.
2. Нажмите `Pull`.
3. Дождитесь завершения обновления.

Если IDEA сообщает, что проект уже обновлён, ничего делать не нужно.

## 8. Подготовка телефонов
К компьютеру можно одновременно подключить один или несколько Android-телефонов.

На каждом телефоне необходимо:
1. Включить режим разработчика.
2. Включить `Отладку по USB`.
3. Подключить телефон к компьютеру кабелем.
4. Разблокировать экран.
5. Подтвердить разрешение на USB-отладку.
6. Установить приложение FitnessOnline.
7. Переключить приложение FitnessOnline на английский язык. Это временная необходимость.

> [!NOTE]
> Для устройств с MIUI или HyperOS одной обычной USB-отладки недостаточно.
> В меню разработчика необходимо включить:
> - `USB debugging`;
> - `Install via USB`;
> - `USB debugging (Security settings)`.

Проверьте подключение в терминале IDEA:
```bash
adb devices
```
Подключённые телефоны должны отображаться со статусом `device`:
```text
List of devices attached
AANBPJMBZ5IRU4JR    device
RFCY60M15XJ        device
```

Фреймворк автоматически определяет каждое подключённое устройство. Копировать UDID и вручную добавлять устройства в настройки проекта не нужно.
Если вместо `device` указан статус `unauthorized`, разблокируйте телефон и подтвердите разрешение на USB-отладку.

## 9. Запуск Appium
В терминале IDEA выполните:
```bash
appium
```

Не закрывайте эту вкладку. Appium должен работать всё время, пока выполняются тесты.

Для команд запуска тестов откройте вторую вкладку терминала кнопкой `+`.
Одного сервера Appium достаточно для всех подключённых Android-устройств.

## 10. Запуск всех Android-тестов
На Mac во второй вкладке терминала IDEA выполните:
```bash
./mvnw test -Pandroid
```

На Windows:
```text
mvnw.cmd test -Pandroid
```

Фреймворк автоматически найдёт все подключённые Android-устройства.
Каждый тест будет запущен на каждом обнаруженном устройстве. Разные устройства выполняют тесты параллельно.

Во время выполнения:
- не отключайте кабели;
- не блокируйте телефоны;
- не закрывайте Appium;
- не управляйте приложением вручную.

Перед каждым тестом данные приложения очищаются автоматически.

## 11. Запуск тестов через IntelliJ IDEA
Отдельный тест или весь тестовый класс можно запустить без Maven-команды.
Перед запуском должны быть подключены телефоны и запущен Appium.

1. В левой части IDEA откройте:
```text
src → test → java → tests → android
```
2. Найдите нужный тест. Названия Android-тестов начинаются со слова `Android`, например:
```text
AndroidGuestAccountCreationTest
```
3. Откройте файл двойным щелчком.
4. Нажмите зелёный треугольник рядом с названием класса, чтобы запустить весь класс.
5. Либо нажмите зелёный треугольник рядом с отдельным тестовым методом.
6. Выберите `Run`.

Фреймворк автоматически запустит выбранный тест на всех подключённых Android-устройствах.
Результат появится во вкладке `Run`.

Файл `BaseAndroidTest` запускать не нужно. Это базовый класс с общими настройками.

## 12. Запуск одного теста командой
Например, для запуска `AndroidGuestAccountCreationTest` на Mac:
```bash
./mvnw test -Pandroid -Dtest=AndroidGuestAccountCreationTest
```

На Windows:
```text
mvnw.cmd test -Pandroid -Dtest=AndroidGuestAccountCreationTest
```

Название после `-Dtest=` заменяется на название нужного тестового класса.
Команда вводится во второй вкладке терминала IDEA.

## 13. Запуск только на выбранном устройстве
По умолчанию тест запускается на всех подключённых Android-устройствах.
Чтобы выполнить его только на одном телефоне, передайте UDID устройства.

Пример на Mac:
```bash
./mvnw test -Pandroid \
  -Dtest=AndroidGuestAccountCreationTest \
  -Dandroid.udid=R58M123456A
```

Пример на Windows:
```text
mvnw.cmd test -Pandroid -Dtest=AndroidGuestAccountCreationTest -Dandroid.udid=R58M123456A
```

В примерах:
- `AndroidGuestAccountCreationTest` — название тестового класса;
- `R58M123456A` — UDID выбранного устройства.

Посмотреть UDID можно командой:
```bash
adb devices
```

## 14. Одновременный запуск Android- и iOS-тестов
Android- и iOS-тесты можно выполнять одновременно через один сервер Appium.
1. Запустите Appium.
2. Запустите нужный Android-тест зелёной стрелкой в IntelliJ IDEA.
3. Не дожидаясь его завершения, запустите нужный iOS-тест другой зелёной стрелкой.

Тесты откроются в отдельных вкладках `Run` и будут выполняться одновременно.

## 15. Результат запуска
При запуске командой результат появится в терминале IDEA.
При запуске зелёной стрелкой результат появится во вкладке `Run`.
Для каждого устройства выводится отдельная строка:
```text
POCO X7 Pro [AANBPJMBZ5IRU4JR] | PASS: 1 | FAIL: 0 | SKIP: 0 | TOTAL: 1
```

Если `FAIL` больше нуля, один или несколько тестов завершились с ошибкой.

При запуске на нескольких устройствах результат одного теста отображается отдельно для каждого телефона. Ошибка на одном устройстве не означает, что тест завершился с ошибкой на остальных.

Некоторые тесты требуют поддержки Google Health. Фреймворк проверяет её автоматически.
Если Google Health недоступен, соответствующий тест будет отмечен как `SKIP` или `Skipped`. Это не ошибка, и дополнительная настройка не требуется.

## 16. Частые проблемы

### Команда `./mvnw` не найдена
Команда введена не в папке проекта.
Откройте терминал через IntelliJ IDEA и убедитесь, что в его строке присутствует:
```text
FitnessOnlineAutoQA
```

### Команда `appium` не найдена
Appium не установлен или IntelliJ IDEA не была перезапущена после установки.
Установите Appium в системном Терминале на Mac или в Командной строке Windows:
```bash
npm install --global appium@3.5.2
```
После этого полностью перезапустите IntelliJ IDEA.

### Команда `npm` не найдена
Node.js не установлен.
Установите Node.js 24 и заново откройте Терминал или Командную строку.

### Команда `adb` не найдена
Не установлены Android SDK Platform-Tools или неправильно настроена переменная `ANDROID_HOME`.
Повторите настройку Android SDK для своей операционной системы.

### Телефон имеет статус `unauthorized`
Разблокируйте телефон и подтвердите разрешение на USB-отладку.
Если запрос не появился:
1. Отключите кабель.
2. На телефоне отзовите разрешения USB-отладки.
3. Подключите кабель снова.
4. Подтвердите новое разрешение.

### Телефон не появился после `adb devices`
Проверьте:
- включена ли отладка по USB;
- разблокирован ли телефон;
- подтверждено ли разрешение на отладку;
- исправен ли кабель;
- выбран ли режим передачи данных;
- не требуется ли USB-драйвер производителя на Windows.

### Один из подключённых телефонов не участвует в запуске
Выполните:
```bash
adb devices
```

Убедитесь, что каждое устройство имеет статус `device`.
Устройства со статусом `unauthorized`, `offline` или без включённой USB-отладки не смогут участвовать в запуске.

### Appium уже запущен
Не запускайте второй сервер. Используйте уже открытую вкладку Appium.
Один сервер обслуживает все параллельные Android-сессии.

### Первый запуск выполняется долго
При первом запуске Maven Wrapper скачивает Maven, а Maven загружает зависимости проекта. Для этого требуется интернет.
Последующие запуски обычно выполняются быстрее.
